import { createClient } from '@libsql/client'; import dotenv from 'dotenv'; dotenv.config({ path: './.env' }); const client = createClient({ url: process.env.DATABASE_URL as string, authToken: process.env.DATABASE_AUTH_TOKEN as string }); async function run() { const r = await client.execute('SELECT invoice_number, store_id FROM transactions ORDER BY invoice_number DESC LIMIT 10'); console.log(r.rows); } run();
